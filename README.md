# AI_Assistant
This is the Future in Software Engineering. AI Assistant that helps Customers via Chat Messages in Real Time by it self.

# Technologies
MongoDB for Vector Search and memory, Seeding DB with AI  Generating product data
Node.js
React
LangChains LangGraph for Agent
AI Google's Gemin

# Commands
npm init //Default all for package JSON
install TS //npm install --save-dev typescript @types/node
Script in package.json //Script that runs our backend, it will run index.ts and also see DB
npm install express mongodb cors @langchain/google-genai @langchain/core
npm install --save-dev ts-node
express for routing
mongodb communicate with our db
Cors
langchain/google-genai //text ai
langchain/core //many things
langchain/langgraph //for vectors ai maybe 
langchain/langgraph-checkpoint-mongodb //saving converstation state
langchain/langgraph-checkpoint-mongodb //working with secret variables
lanchain
zod //schema validation library
cors

# Commands
Install dependencies: cd Server && npm install
Add missing dev dependency: npm install --save-dev ts-node
Seed database: npm run seed (after adding the script)
Start server: npm run dev (after adding the script)